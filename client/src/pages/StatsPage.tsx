/**
 * Stats (`/stats`): placeholder shell only. Its content — aggregate stats over
 * all imported Games (game count and win/draw/loss tally) — is owned by US-6.
 */
export function StatsPage() {
  return (
    <section aria-labelledby="stats-heading">
      <h2 id="stats-heading">Stats</h2>
      <p>À venir : statistiques globales sur l'ensemble de vos parties.</p>
    </section>
  );
}
